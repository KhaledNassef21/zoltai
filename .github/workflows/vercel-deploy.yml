name: Vercel Production Deploy

# Single source of truth for production deploys.
# Replaces the Vercel-GitHub auto-integration to guarantee domain aliases stay in sync.
# Flow: pull env -> build -> deploy -> alias (one atomic job).

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: vercel-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Pull Vercel Environment (production)
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build Project Artifacts
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy Prebuilt Artifacts to Vercel
        id: deploy
        run: |
          DEPLOY_URL=$(vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }} 2>&1 | tail -1)
          echo "url=$DEPLOY_URL" >> $GITHUB_OUTPUT
          echo "✅ Deployed: $DEPLOY_URL"

      - name: Assign Domain Aliases
        run: |
          echo "Aliasing to zoltai.org + www.zoltai.org..."
          vercel alias set ${{ steps.deploy.outputs.url }} zoltai.org \
            --token=${{ secrets.VERCEL_TOKEN }} \
            --scope=${{ secrets.VERCEL_ORG_ID }}
          vercel alias set ${{ steps.deploy.outputs.url }} www.zoltai.org \
            --token=${{ secrets.VERCEL_TOKEN }} \
            --scope=${{ secrets.VERCEL_ORG_ID }}
          echo "✅ zoltai.org + www.zoltai.org -> ${{ steps.deploy.outputs.url }}"
